name: test

on:
  pull_request:
    branches:
      - main
      - backend

jobs:
  Run-PR-Test:
    runs-on: self-hosted
    permissions:
      contents: read
      checks: write
    steps: # steps 키워드가 추가되어야 합니다.
      - name: test
        run: |
          echo ls
