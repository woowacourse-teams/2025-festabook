<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="SearchResult"
            type="com.daedan.festabook.presentation.explore.model.SearchResultUiModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?android:attr/selectableItemBackground"
        android:paddingHorizontal="28dp"
        android:paddingVertical="8dp">

        <TextView
            android:id="@+id/tv_university_name"
            style="@style/PretendardMedium14"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/gray800"
            android:text="@{SearchResult.universityName}"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="서울시립대학교" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            style="@style/PretendardRegular12"
            tools:text="축제 이름"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/gray600"
            android:text="@{SearchResult.festivalName}"
            app:layout_constraintTop_toBottomOf="@id/tv_university_name"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>