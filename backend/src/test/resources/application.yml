spring:
  application:
    name: festabook
  jpa:
    open-in-view: false
    defer-datasource-initialization: true
  datasource:
    driver-class-name: org.h2.Driver

# Swagger
springdoc:
  swagger-ui:
    path: /api-docs
    persist-authorization: true

firebase:
  adminsdk:
    account:
      path: firebase/firebase-adminsdk-account.json
